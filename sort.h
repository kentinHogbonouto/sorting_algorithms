#ifndef SORT_H
#define SORT_H

#include <stddef.h> 
#include <stdlib.h>

/**
*File name: sort.h
*
*sort.h contain all the function defintion that will be use throught this project
*/

void bubble_sort(int *array, size_t size);
void print_array(const int *array, size_t size);

#endif
